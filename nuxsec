#!/usr/bin/env bash

set -euo pipefail

NUXSEC_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
NUXSEC_BIN="${NUXSEC_ROOT}/build/bin/nuxsec"

if [ ! -x "${NUXSEC_BIN}" ]; then
  echo "nuxsec executable not found at ${NUXSEC_BIN}. Run 'make' first." >&2
  exit 1
fi

exec "${NUXSEC_BIN}" "$@"
