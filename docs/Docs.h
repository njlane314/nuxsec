/**
 *  @file  docs/Docs.h
 *
 *  @brief  Extra doxygen documentation
 */

// -----------------------------------------------------------------------------------------------------------------------------------------

/**
 *  @namespace nuxsec
 *
 *  @brief  The main nuxsec namespace
 */

// -----------------------------------------------------------------------------------------------------------------------------------------

/**
 *  @namespace nuxsec_macros
 *
 *  @brief  The nuxsec macros namespace
 */

// -----------------------------------------------------------------------------------------------------------------------------------------

/**
 *  @mainpage
 *
 *  @section overview Overview
 *
 *  nuxsec is a focused C++ toolkit for building secure, auditable software systems.
 *
 *  @section structure Code structure
 *
 *  - @ref dir_bin "Executables (bin/)" for runnable entry points.
 *  - @ref dir_lib "Libraries (lib/)" for reusable building blocks.
 *  - @ref namespaces "Namespaces" for module-level organization.
 *  - @ref files "Files" for implementation references and extra documentation.
 *  - @ref dirs "Directories" for the full source tree layout.
 *
 *  @section explore Explore the API
 *
 *  Start with the core classes, or jump straight to the repository indices.
 *
 *  - @ref nuio::ArtProvenanceIO "nuio::ArtProvenanceIO" for provenance IO helpers.
 *  - @ref RunInfoDB "RunInfoDB" for aggregating run metadata from SQLite.
 *  - @ref classes "Class index" to browse everything alphabetically.
 *  - @ref namespaces "Namespace index" for module-level entry points.
 *  - @ref files "File index" for implementation details.
 *  - @ref dirs "Directory index" for the full source tree layout.
 *
 */
